ifeq "$(strip $(CC))" "icc"
CXX=icpc
CXXFLAGS= -O0 -fPIC -shared-intel ${DBGFLAGS}
LDFLAGS=
else
CXX=g++
CXXFLAGS= -std=c++11 -pthread -O0 -fPIC ${DBGFLAGS}
LDFLAGS= -pthread
endif

INCDIR= `pkg-config blitz --cflags` -I$(UAL_BUILD)/lowlevel -I$(UAL_BUILD)/cppinterface/src
LIBS= -L$(UAL_BUILD)/lowlevel -L$(UAL_BUILD)/cppinterface/lib
LIBS+= -lal-cpp -lal `pkg-config blitz --libs`
BINOUT=TestSuite

